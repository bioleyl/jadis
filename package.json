{
  "private": true,
  "scripts": {
    "build:helpers": "npm run build --workspace=packages/helpers",
    "publish:core": "npm publish --workspace=packages/core --access public",
    "publish:router": "npm publish --workspace=packages/router --access public",
    "build:publish": "npm run build:helpers && npm publish:core && npm publish:router",
    "test": "jest"
  },
  "workspaces": [
    "./packages/helpers",
    "./packages/core",
    "./packages/router"
  ],
  "devDependencies": {
    "@rollup/plugin-commonjs": "^28.0.3",
    "@rollup/plugin-node-resolve": "^16.0.1",
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "rollup": "^4.40.0",
    "rollup-plugin-dts": "^6.2.1",
    "ts-jest": "^29.3.2",
    "typescript": "^5.8.3"
  },
  "name": "",
  "version": ""
}

# Jadis vanilla template

This project uses Jadis with native ES modules and `createElement`. It does not use JSX, a bundler, or a build step.

Serve the project directory with any static HTTP server, for example:

```bash
npx serve .
```

Then open the displayed local URL in your browser.
